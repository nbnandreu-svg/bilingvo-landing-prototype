# Include inside an existing HTTPS server/location serving the Bilingvo HTML.
# Preserve existing CSP directives and other add_header settings at that level.
# This is a deployment template, not a configuration applied to Tilda/GitHub.
add_header Content-Security-Policy "frame-ancestors 'none'" always;
add_header X-Frame-Options "DENY" always;
